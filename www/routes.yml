/:
  controller: main
  action: home

/contact:
  controller: main
  action: contact

/dashboard:
  controller: admin
  action: dashboard
  security:
    - admin
    - user

# Authenticator route

/login:
  controller: authenticator
  action: login

/logout:
  controller: authenticator
  action: logout

/register:
  controller: authenticator
  action: register

/forget:
  controller: authenticator
  action: pwdforget

# User controller route
/users:
  controller: user
  action: index

/user/show:
  controller: user
  action: show

/user/edit:
  controller: user
  action: edit

/user/update:
  controller: user
  action: update

/user/delete:
  controller: user
  action: delete

/verify-account:
  controller: authenticator
  action: verifyAccount